# 视力中心蓝牙数据汇聚系统 - 产品需求文档

**文档版本**: v3.0
**创建日期**: 2026-05-23
**更新日期**: 2026-05-23
**产品负责人**: [待填写]

---

## 0. 历史项目经验总结

### 0.1 历史项目回顾

**项目性质**: 视力检查数据打印服务（前后端分离）

**存在问题**:
| 问题类别 | 具体表现 |
|---------|---------|
| **架构问题** | 代码重复（app.py 和 printing/manager.py）、模块化不完整、职责不清 |
| **代码质量** | 硬编码、魔法数字、错误处理不完善、缺少测试、配置分散 |
| **功能稳定性** | 平台兼容性问题、打印队列管理不健壮、状态同步不及时、缺少重连机制 |

### 0.2 本次项目的改进原则

针对历史经验，**本项目必须遵循以下设计原则**：

| 原则         | 具体要求                           | 目的         |
| ---------- | ------------------------------ | ---------- |
| **分层清晰**   | API层 → 业务逻辑层 → 设备抽象层 → 硬件抽象层   | 避免职责混杂     |
| **模块化完整**  | 打印机驱动、蓝牙适配器、打印服务、设备管理、数据处理分离   | 便于独立测试和维护  |
| **避免硬编码**  | ESC/POS 命令封装成有意义的方法调用、配置统一管理   | 提高可读性和可维护性 |
| **错误处理完善** | 自定义异常体系、具体错误分类处理、详细日志记录        | 便于调试和问题定位  |
| **平台兼容性**  | 蓝牙连接统一抽象接口、Windows/Linux 多平台适配 | 降低移植成本     |
| **队列健壮性**  | 打印队列管理、状态同步及时、自动重连机制           | 提升系统稳定性    |
| **可测试性**   | 各层独立可测试、支持模拟设备用于测试             | 保证代码质量     |

### 0.3 关键技术决策（基于历史经验）

| 决策项      | 方案                   | 理由      |
| -------- | -------------------- | ------- |
| **配置管理** | 统一配置模块、支持环境变量        | 避免配置分散  |
| **日志系统** | 结构化日志、分级记录           | 便于分析问题  |
| **错误处理** | 自定义异常体系              | 清晰的错误层级 |
| **测试框架** | 必须有单元测试              | 质量保证    |
| **队列管理** | Queue + Thread       | 无需额外依赖  |
| **打印命令** | 封装 ESC/POS 命令为有意义的方法 | 避免魔法数字  |

---

## 1. 问题陈述

目前视力中心有多款专业设备（电子视力表、眼底照相机、电脑验光仪、非接触式眼压计、眼生物测量仪等），各设备采用不同的数据格式和通讯协议，导致：

- **数据汇聚困难**: 各设备数据孤立，无法自动统一归集
- **人工录入繁琐**: 医护人员需要手动转录数据，效率低下
- **数据一致性差**: 人工操作容易产生错误，影响诊疗质量
- **打印格式不统一**: 各设备的打印输出格式各异，患者体验不佳

这些问题严重影响了视力中心的诊疗效率和数据管理水平，亟需一个统一的解决方案。

---

## 2. 目标

本产品旨在通过一个综合蓝牙设备，实现以下目标：

1. **统一数据接入**: 对接所有视力中心设备，实现数据自动采集
2. **简化工作流程**: 减少医护人员手动录入数据的工作量
3. **提升数据质量**: 通过自动化降低人为错误，确保数据准确性
4. **统一打印输出**: 提供标准化的打印格式和二维码功能
5. **改善用户体验**: 为医护人员和患者提供更顺畅的使用体验

---

## 3. 非目标

以下内容明确不在本版本的范围内：

1. **设备通讯协议深度改造** - 不修改现有设备的底层通讯协议，仅利用其蓝牙打印接口
2. **云端数据存储** - 本版本仅实现本地数据汇聚和打印，不涉及云端同步
3. **历史数据分析** - 不提供数据趋势分析、统计报表等高级功能
4. **设备控制功能** - 仅接收数据，不实现对设备的反向控制
5. **多中心管理** - 仅支持单视力中心部署，不涉及多机构级联管理

---

## 4. 用户故事

### 4.1 视力检查医生

| ID     | 用户故事                                              | 优先级 |
| ------ | ------------------------------------------------- | --- |
| US-001 | 作为视力检查医生，我希望在完成设备检查后，数据能自动汇聚到系统中，以便我无需手动转录数据，节省时间 | P0  |
| US-002 | 作为视力检查医生，我希望能一键打印标准化的检查报告，以便患者获得清晰、统一格式的检查结果      | P0  |
| US-003 | 作为视力检查医生，我希望打印的报告包含数据二维码，以便后续系统能快速扫码获取历史数据        | P1  |

### 4.2 设备管理员

| ID     | 用户故事                                       | 优先级 |
| ------ | ------------------------------------------ | --- |
| US-004 | 作为设备管理员，我希望能方便地添加新设备到系统中，以便视力中心采购新设备后能快速接入 | P1  |
| US-005 | 作为设备管理员，我希望能监控各设备的数据连接状态，以便及时发现和解决连接问题     | P1  |

### 4.3 患者

| ID     | 用户故事                                      | 优先级 |
| ------ | ----------------------------------------- | --- |
| US-006 | 作为患者，我希望获得格式统一、清晰易读的检查报告，以便我能更好地理解自己的视力状况 | P1  |
| US-007 | 作为患者，我希望报告上有二维码，以便我后续复诊时医生能快速获取我的历史检查数据   | P2  |

---

## 5. 需求

### 5.1 Must-Have (P0) - 必须具备

| ID       | 需求描述                        | 详细功能说明                              | 验收标准                              |
| -------- | --------------------------- | ----------------------------------- | --------------------------------- |
| REQ-001  | 支持对接电子视力表，通过 HTTP 协议获取数据    | **双向通信模式**：<br>1. 系统启动 HTTP 服务监听指定端口<br>2. 支持接收设备自动推送的测试数据<br>3. 支持主动从设备拉取历史数据<br>4. 支持 JSON 和 URL 参数两种数据格式 | 电子视力表数据能正确解析并显示在系统中               |
| REQ-001a | 支持电子视力表自动推送模式（HTTP GET 回调）  | **回调配置**：<br>1. 系统提供配置界面设置回调地址<br>2. 自动解析 URL 参数（visionType, eyes, right, left, resultTime, userName, userId, deviceNumber）<br>3. 数据接收后自动存入本地暂存区<br>4. 支持数据接收确认机制 | 视力表测试完成后能自动发送数据到系统配置的 HTTP 服务器地址  |
| REQ-001b | 支持电子视力表主动拉取模式（HTTP JSON 接口） | **拉取功能**：<br>1. 配置设备 IP 地址和端口<br>2. 支持按时间范围拉取历史数据<br>3. 支持单次拉取和批量拉取<br>4. 解析 JSON 格式数据并映射到统一数据模型 | 系统能通过 HTTP 主动从视力表获取历史数据（JSON 格式）  |
| REQ-002  | 支持对接眼底照相机，通过蓝牙打印协议获取数据      | **蓝牙打印协议解析**：<br>1. 模拟蓝牙 SPP 打印机角色<br>2. 接收设备发送的打印数据流<br>3. 从打印流中提取结构化数据（患者信息、检查结果）<br>4. 支持数据完整性校验 | 眼底照相机数据能正确解析并显示在系统中               |
| REQ-003  | 支持对接电脑验光仪，通过蓝牙打印协议获取数据      | **蓝牙打印协议解析**：<br>1. 模拟蓝牙 SPP 打印机角色<br>2. 接收设备发送的打印数据流<br>3. 从打印流中提取验光数据（球镜、柱镜、轴位等）<br>4. 支持左右眼数据分别解析 | 电脑验光仪数据能正确解析并显示在系统中               |
| REQ-004  | 支持对接非接触式眼压计，通过蓝牙打印协议获取数据    | **蓝牙打印协议解析**：<br>1. 模拟蓝牙 SPP 打印机角色<br>2. 接收设备发送的打印数据流<br>3. 从打印流中提取眼压数据<br>4. 支持多次测量结果的识别和记录 | 眼压计数据能正确解析并显示在系统中                 |
| REQ-005  | 支持对接眼生物测量仪，通过蓝牙打印协议获取数据     | **蓝牙打印协议解析**：<br>1. 模拟蓝牙 SPP 打印机角色<br>2. 接收设备发送的打印数据流<br>3. 从打印流中提取生物测量数据（眼轴长度、角膜曲率等）<br>4. 支持详细协议文档中的所有字段 | 眼生物测量仪数据能正确解析并显示在系统中              |
| REQ-006  | 将各设备数据转换为统一的内部数据格式          | **数据映射引擎**：<br>1. 定义统一的数据模型（患者信息、检查时间、设备类型、左右眼数据等）<br>2. 各设备有独立的数据解析器<br>3. 支持字段映射配置<br>4. 数据类型自动转换（字符串、数字、日期等） | 不同设备的同类数据字段能正确映射                  |
| REQ-007  | 支持连接蓝牙热敏打印机                 | **打印机连接管理**：<br>1. 蓝牙设备发现和配对<br>2. 打印机状态监控（在线/离线、缺纸、电量等）<br>3. 连接状态自动检测<br>4. 支持多台打印机配置（可选） | 系统能成功连接并配对蓝牙热敏打印机                 |
| REQ-008  | 按自定义格式打印检查报告                | **打印模板引擎**：<br>1. 56mm 宽度标准模板<br>2. 标题居中放大显示<br>3. 患者信息区域（姓名、时间）<br>4. 各设备数据分块展示<br>5. 支持多设备数据合并打印<br>6. 对齐、加粗、字体大小等格式控制 | 打印输出符合预设的统一格式规范                   |
| REQ-009  | 打印报告包含数据二维码                 | **二维码生成**：<br>1. JSON 格式数据编码<br>2. QR Code 版本 2-3（1.5-2cm 见方）<br>3. 模块大小 6-8dot<br>4. 纠错等级 M 或 Q<br>5. 二维码居中打印<br>6. 底部添加"扫码查看完整检查数据"提示 | 打印的二维码能被扫描并还原为完整检查数据              |
| REQ-010  | 支持手动触发打印                    | **打印操作**：<br>1. 界面提供打印按钮<br>2. 支持选择当前数据或历史数据<br>3. 显示打印预览（可选 P1）<br>4. 打印状态反馈（正在打印/完成/失败） | 用户可在界面上点击打印按钮输出报告                 |
| REQ-010a | 支持自动触发打印                    | **自动打印**：<br>1. 设备数据接收后自动触发打印（可配置开关）<br>2. 支持延迟打印（等待多个设备数据）<br>3. 打印失败自动重试 | 设备数据接收后可自动打印检查报告（需配置开启） |
| REQ-020  | **架构分层清晰**                  | **分层架构**：<br>1. API 层（RESTful API、Web UI）<br>2. 业务逻辑层（打印服务、设备服务、数据处理服务、队列服务）<br>3. 设备抽象层（打印机驱动、设备管理器）<br>4. 硬件抽象层（蓝牙适配器、串口适配器）<br>5. 基础设施层（配置、日志、异常、队列） | API层、业务逻辑层、设备抽象层、硬件抽象层分离明确        |
| REQ-021  | **ESC/POS 命令封装**            | **命令封装**：<br>1. 初始化、对齐、加粗、字体大小、QR码、切纸等常用命令<br>2. 所有魔法数字替换为有意义的方法名<br>3. 链式调用支持<br>4. 命令缓冲区管理 | 所有打印命令封装成有意义的方法调用，无魔法数字           |
| REQ-022  | **统一配置管理**                  | **配置模块**：<br>1. 集中配置文件（JSON/YAML）<br>2. 环境变量覆盖支持<br>3. 运行时配置更新（部分参数）<br>4. 配置验证和默认值<br>5. 配置热重载支持（可选） | 配置集中管理，支持环境变量覆盖，无分散配置             |
| REQ-023  | **完善的错误处理**                 | **异常体系**：<br>1. 自定义异常类（DeviceConnectionError, DataParseError, PrintError 等）<br>2. 错误分类和分级<br>3. 详细日志记录（错误类型、堆栈、上下文）<br>4. 用户友好的错误提示<br>5. 错误恢复策略 | 自定义异常体系，错误分类记录，详细日志输出             |
| REQ-024  | **打印队列管理**                  | **队列服务**：<br>1. FIFO 队列结构<br>2. 队列状态持久化（防止重启丢失）<br>3. 打印任务状态跟踪（等待/打印中/完成/失败）<br>4. 失败自动重试（可配置次数和间隔）<br>5. 队列优先级支持（可选） | 支持打印队列，状态实时同步，失败自动重试              |
| REQ-025  | **蓝牙自动重连机制**                | **重连策略**：<br>1. 连接状态实时监控<br>2. 断开后自动触发重连<br>3. 最大重试次数可配置<br>4. 重试间隔递增（避免频繁重试）<br>5. 重连成功/失败通知 | 连接断开后自动重连，最大重试次数可配置               |
| REQ-026  | **平台兼容性**                   | **多平台支持**：<br>1. 统一蓝牙适配器抽象接口<br>2. Windows 平台实现（使用 Windows.Devices.Bluetooth 或第三方库）<br>3. Linux 平台实现（使用 PyBluez 或 bluez）<br>4. 平台自动检测和适配 | 统一蓝牙适配器接口，同时支持 Windows 和 Linux 平台 |
| REQ-027  | **单元测试覆盖**                  | **测试要求**：<br>1. 打印机驱动模块测试覆盖率 ≥ 80%<br>2. 数据处理模块测试覆盖率 ≥ 80%<br>3. 核心业务逻辑测试覆盖率 ≥ 70%<br>4. 使用模拟设备进行集成测试 | 核心模块（打印机驱动、数据处理）有单元测试             |
| REQ-028  | **支持模拟设备**                  | **模拟接口**：<br>1. 模拟电子视力表 HTTP 服务<br>2. 模拟蓝牙打印机数据生成器<br>3. 模拟热敏打印机（输出到文件/控制台）<br>4. 支持回放真实数据样本<br>5. 便于开发和测试环境使用 | 提供模拟设备接口，便于无硬件环境下的测试              |

### 5.2 Nice-to-Have (P1) - 应该具备

| ID      | 需求描述                   | 详细功能说明                              | 验收标准                    |
| ------- | ---------------------- | ----------------------------------- | ----------------------- |
| REQ-011 | 设备连接状态可视化显示            | **状态监控**：<br>1. 实时显示各设备连接状态（在线/离线/连接中/错误）<br>2. 显示最后数据接收时间<br>3. 显示信号强度（蓝牙设备）<br>4. 异常状态告警提示（颜色/图标）<br>5. 状态历史记录 | 界面能清晰显示各设备当前连接状态（在线/离线） |
| REQ-012 | 支持设备管理功能（添加、编辑、删除设备配置） | **设备管理**：<br>1. 设备列表展示<br>2. 添加新设备向导（选择类型、配置连接参数）<br>3. 编辑设备配置<br>4. 删除设备配置（确认机制）<br>5. 设备连接测试功能<br>6. 设备分组支持（可选） | 用户可通过界面管理已配置的设备         |
| REQ-013 | 本地数据暂存功能               | **数据管理**：<br>1. 本地 SQLite 数据库存储<br>2. 支持按时间范围查询<br>3. 支持按患者姓名/ID 查询<br>4. 支持按设备类型筛选<br>5. 数据导出功能（JSON/CSV）<br>6. 数据清理策略（保留最近 N 天） | 系统能暂存最近 N 条检查数据供查询      |
| REQ-014 | 支持打印预览                 | **预览功能**：<br>1. 打印前在界面显示报告预览<br>2. 模拟 56mm 宽度显示效果<br>3. 支持缩放查看<br>4. 预览内容与实际打印一致 | 用户可在打印前查看报告效果           |
| REQ-015 | 支持多种打印模板选择             | **模板管理**：<br>1. 预设多种打印模板（简洁版、完整版、定制版）<br>2. 模板切换界面<br>3. 自定义模板编辑（可选 P2）<br>4. 模板预览功能 | 用户可根据需要切换不同的报告模板        |
| REQ-015a | 支持打印历史记录 | **打印历史**：<br>1. 记录每次打印操作（时间、数据、状态）<br>2. 支持重新打印历史数据<br>3. 打印次数统计<br>4. 打印失败记录和重试 | 系统能记录和查询打印历史记录 |

### 5.3 性能需求

| ID      | 需求描述                   | 详细说明                              | 目标值                    |
| ------- | ---------------------- | ----------------------------------- | ----------------------- |
| PERF-001 | 数据接收响应时间 | 从设备发送数据到系统完成解析的时间 | ≤ 2秒 |
| PERF-002 | 打印任务响应时间 | 从点击打印到打印机开始输出的时间 | ≤ 3秒 |
| PERF-003 | 单次打印完成时间 | 从开始打印到切纸完成的时间 | ≤ 10秒 |
| PERF-004 | 系统启动时间 | 从启动到所有服务就绪的时间 | ≤ 30秒 |
| PERF-005 | 并发处理能力 | 同时接收多个设备数据的处理能力 | ≥ 5个设备并发 |
| PERF-006 | 数据查询响应时间 | 本地历史数据查询响应时间 | ≤ 1秒（1000条数据内） |
| PERF-007 | 内存占用 | 系统运行时内存占用 | ≤ 512MB |
| PERF-008 | 存储空间 | 本地数据存储容量 | ≥ 10万条检查记录 |

### 5.4 安全与合规需求

| ID      | 需求描述                   | 详细说明                              |
| ------- | ---------------------- | ----------------------------------- |
| SEC-001 | 患者数据隐私保护 | 1. 本地数据加密存储（可选）<br>2. 敏感信息脱敏显示<br>3. 数据访问日志记录<br>4. 符合医疗数据隐私相关法规 |
| SEC-002 | 系统访问控制 | 1. 基础的用户认证（可选）<br>2. 操作日志记录<br>3. 配置修改权限控制 |
| SEC-003 | 数据完整性 | 1. 接收数据校验<br>2. 打印数据确认<br>3. 数据备份机制（可选） |
| SEC-004 | 网络安全 | 1. HTTP 接口支持 HTTPS（可选）<br>2. 蓝牙连接安全配对<br>3. 防止未授权设备连接 |

### 5.5 用户体验需求

| ID      | 需求描述                   | 详细说明                              |
| ------- | ---------------------- | ----------------------------------- |
| UX-001 | 界面简洁直观 | 1. 主界面清晰显示当前待打印数据<br>2. 设备状态一目了然<br>3. 操作流程简单（1-2步完成打印） |
| UX-002 | 状态反馈及时 | 1. 数据接收成功/失败提示<br>2. 打印进度显示<br>3. 错误信息清晰易懂<br>4. 操作结果确认 |
| UX-003 | 配置简单 | 1. 设备配置向导式界面<br>2. 提供默认配置<br>3. 配置参数有清晰说明<br>4. 支持配置导入导出（可选） |
| UX-004 | 异常处理友好 | 1. 网络断开自动重连并提示<br>2. 打印机缺纸/故障明确提示<br>3. 数据解析失败保留原始数据供排查<br>4. 提供常见问题排查指南 |

### 5.6 运维与监控需求

| ID      | 需求描述                   | 详细说明                              |
| ------- | ---------------------- | ----------------------------------- |
| OPS-001 | 日志系统 | 1. 分级日志（DEBUG/INFO/WARNING/ERROR）<br>2. 结构化日志格式（JSON）<br>3. 日志滚动和保留策略<br>4. 关键操作日志记录 |
| OPS-002 | 系统监控 | 1. 服务健康检查接口<br>2. 设备状态监控<br>3. 打印队列监控<br>4. 资源使用监控（CPU/内存/磁盘） |
| OPS-003 | 故障诊断 | 1. 系统自检功能<br>2. 设备连接测试工具<br>3. 打印测试功能<br>4. 诊断报告生成（可选） |
| OPS-004 | 版本管理 | 1. 版本号显示<br>2. 更新检查（可选）<br>3. 配置版本兼容处理 |

### 5.3 Future Considerations (P2) - 未来考虑

| ID      | 需求描述      | 备注       |
| ------- | --------- | -------- |
| REQ-016 | 云端数据同步    | 考虑后续版本实现 |
| REQ-017 | 历史数据统计分析  | 考虑后续版本实现 |
| REQ-018 | 多中心级联管理   | 考虑后续版本实现 |
| REQ-019 | 患者报告小程序查看 | 考虑后续版本实现 |

---

## 6. 成功指标

### 6.1 领先指标（Leading Indicators）

| 指标       | 目标值   | 测量方式              |
| -------- | ----- | ----------------- |
| 设备接入成功率  | ≥ 95% | 成功接入的设备数 / 总设备数   |
| 数据解析准确率  | ≥ 98% | 正确解析的数据条数 / 总数据条数 |
| 单次打印操作耗时 | ≤ 10秒 | 从点击打印到输出完成的平均时间   |
| 医护人员接受度  | ≥ 90% | 用户满意度调研得分         |

### 6.2 滞后指标（Lagging Indicators）

| 指标              | 目标值   | 测量方式     |
| --------------- | ----- | -------- |
| 人工数据录入时间减少      | ≥ 60% | 实施前后对比   |
| 数据错误率降低         | ≥ 80% | 实施前后对比   |
| 报告打印标准化率        | 100%  | 标准格式报告占比 |
| 用户（医护）净推荐值（NPS） | ≥ 40  | 用户调研     |

---

## 7. 开放问题

| ID      | 问题                               | 状态      | 负责人     | 期望解决时间 |
| ------- | -------------------------------- | ------- | ------- | ------ |
| OQ-001a | 电子视力表协议文档已获取                     | ✅ 已解决   | 研发团队    | 已完成    |
| OQ-001b | 眼生物测量仪协议文档已获取                    | ✅ 已解决   | 研发团队    | 已完成    |
| OQ-001c | 视力筛查仪协议文档已获取                     | ✅ 已解决   | 研发团队    | 已完成    |
| OQ-001d | 眼底照相机蓝牙打印协议具体规范是什么？是否有技术文档？      | ⏳ 待解决   | 研发团队    | 项目启动前  |
| OQ-001e | 电脑验光仪蓝牙打印协议具体规范是什么？是否有技术文档？      | ⏳ 待解决   | 研发团队    | 项目启动前  |
| OQ-001f | 非接触式眼压计蓝牙打印协议具体规范是什么？是否有技术文档？    | ⏳ 待解决   | 研发团队    | 项目启动前  |
| OQ-002  | 目标蓝牙热敏打印机的选型标准和推荐型号是什么？          | ✅ 已解决   | 硬件团队    | 已完成    |
| OQ-003  | 医疗数据相关的合规要求（如数据隐私、医疗器械注册等）具体有哪些？ | ⏳ 待解决   | 合规团队    | 方案设计阶段 |
| OQ-004  | 统一数据格式的具体字段定义（除电子视力表外的其他设备）      | 🔄 部分解决 | 产品团队    | 需求评审前  |
| OQ-005  | 二维码的数据结构和编码方式如何定义？               | ✅ 已解决   | 产品 + 研发 | 已完成    |

---

## 8. 时间线考虑

### 8.1 关键里程碑

| 里程碑         | 目标时间       | 交付物            |
| ----------- | ---------- | -------------- |
| M1 - 需求确认   | 2026-06-07 | 已评审通过的 PRD     |
| M2 - 技术方案设计 | 2026-06-21 | 技术架构设计文档       |
| M3 - 设备协议解析 | 2026-07-12 | 各设备数据解析模块      |
| M4 - 核心功能开发 | 2026-07-26 | 数据汇聚、格式转换、打印功能 |
| M5 - 系统联调测试 | 2026-08-09 | 可运行的完整系统       |
| M6 - 现场部署验证 | 2026-08-23 | 试点视力中心上线       |

### 8.2 依赖关系

- **前置依赖**：各设备蓝牙打印协议技术文档获取
- **内部依赖**：硬件选型完成、数据格式定义确认
- **外部依赖**：试点视力中心配合、合规审查通过

---

## 9. 风险与应对（基于历史经验）

| 风险                   | 可能性 | 影响  | 应对策略                              |
| -------------------- | --- | --- | --------------------------------- |
| 部分设备蓝牙打印协议不规范，无法正确解析 | 中   | 高   | 提前开展设备预研，必要时与设备厂商沟通获取技术支持         |
| 医疗合规要求超出预期，导致项目延期    | 中   | 中   | 尽早邀请合规团队介入，确保方案设计符合法规要求           |
| **蓝牙连接稳定性不足**        | 高   | 高   | **分层设计 + 自动重连机制 + 完整状态同步**        |
| 各设备数据字段差异大，统一格式设计困难  | 高   | 中   | 深入调研各设备数据结构，设计灵活可扩展的数据模型          |
| **平台兼容性问题**          | 中   | 高   | **蓝牙适配器抽象接口，Windows/Linux 双平台实现** |
| **打印队列管理不健壮**        | 中   | 高   | **独立队列服务，状态持久化，失败自动重试**           |
| **代码架构再次混乱**         | 中   | 高   | **严格分层设计，代码审查把关，避免重复代码**          |
| **缺少测试导致质量问题**       | 中   | 中   | **单元测试强制要求，模拟设备支持无硬件测试**          |

---

## 10. 技术方案与设备对接细节

### 10.1 系统架构概述（基于历史经验优化）

本系统采用**分层架构设计**，支持**蓝牙打印协议**和**HTTP 协议**两种数据接入方式：

```
┌─────────────────────────────────────────────────────────────────┐
│              视力中心蓝牙数据汇聚系统 - 分层架构                  │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  API 层 (Interface Layer)                                 │   │
│  │  - RESTful API 接口                                       │   │
│  │  - Web UI 操作界面                                        │   │
│  └────────────────────────┬─────────────────────────────────┘   │
│                           │                                     │
│  ┌────────────────────────▼──────────────────────────────────┐  │
│  │  业务逻辑层 (Service Layer)                               │  │
│  │  ┌─────────────────────┐ ┌─────────────────────┐         │  │
│  │  │  PrintingService   │ │  DeviceService      │         │  │
│  │  │  打印服务          │ │  设备管理服务       │         │  │
│  │  └─────────────────────┘ └─────────────────────┘         │  │
│  │  ┌─────────────────────┐ ┌─────────────────────┐         │  │
│  │  │  DataProcessService│ │  QueueService       │         │  │
│  │  │  数据处理服务      │ │  队列管理服务       │         │  │
│  │  └─────────────────────┘ └─────────────────────┘         │  │
│  └────────────────────────┬─────────────────────────────────┘   │
│                           │                                     │
│  ┌────────────────────────▼──────────────────────────────────┐  │
│  │  设备抽象层 (Device Abstraction Layer)                     │  │
│  │  ┌─────────────────────┐ ┌─────────────────────┐         │  │
│  │  │  PrinterDriver     │ │  DeviceManager      │         │  │
│  │  │  打印机驱动        │ │  设备管理器         │         │  │
│  │  └─────────────────────┘ └─────────────────────┘         │  │
│  └────────────────────────┬─────────────────────────────────┘   │
│                           │                                     │
│  ┌────────────────────────▼──────────────────────────────────┐  │
│  │  硬件抽象层 (Hardware Abstraction Layer)                   │  │
│  │  ┌─────────────────────┐ ┌─────────────────────┐         │  │
│  │  │  BluetoothAdapter  │ │  SerialAdapter      │         │  │
│  │  │  蓝牙适配器 (抽象) │ │  串口适配器 (可选)  │         │  │
│  │  └─────────────────────┘ └─────────────────────┘         │  │
│  │  ┌─────────────────────┐ ┌─────────────────────┐         │  │
│  │  │ WindowsBluetoothImpl│ │ LinuxBluetoothImpl  │         │  │
│  │  │ Windows 蓝牙实现    │ │ Linux 蓝牙实现      │         │  │
│  │  └─────────────────────┘ └─────────────────────┘         │  │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │  基础设施层 (Infrastructure Layer)                       │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐    │   │
│  │  │ Config   │ │  Logger  │ │  Exception│ │  Queue   │    │   │
│  │  │ 配置管理 │ │ 日志系统 │ │ 异常处理 │ │ 队列管理 │    │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘    │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

**关键设计模式**:
| 模式 | 应用场景 | 目的 |
|------|---------|------|
| **策略模式** | 不同平台的蓝牙连接策略（Windows/Linux） | 灵活切换平台实现 |
| **工厂模式** | 打印机驱动工厂、设备适配器工厂 | 统一创建接口 |
| **命令模式** | ESC/POS 打印命令封装 | 将魔法数字变为有意义的调用 |
| **观察者模式** | 数据接收后的回调机制 | 实现自动打印等事件响应 |

### 10.1.1 模块划分与职责

| 模块                           | 职责                 | 文件位置建议    |
| ---------------------------- | ------------------ | --------- |
| `printer_driver.py`          | 打印机驱动，ESC/POS 命令封装 | drivers/  |
| `bluetooth_adapter.py`       | 蓝牙适配器抽象接口          | adapters/ |
| `windows_bluetooth.py`       | Windows 蓝牙实现       | adapters/ |
| `linux_bluetooth.py`         | Linux 蓝牙实现         | adapters/ |
| `printing_service.py`        | 打印服务，业务逻辑          | services/ |
| `device_service.py`          | 设备管理服务             | services/ |
| `data_processing_service.py` | 数据处理服务             | services/ |
| `queue_service.py`           | 队列管理服务             | services/ |
| `config.py`                  | 配置管理               | core/     |
| `exceptions.py`              | 自定义异常体系            | core/     |
| `logger.py`                  | 日志系统               | core/     |

### 10.1.2 ESC/POS 命令封装设计

**目标**: 将魔法数字 → 有意义的方法调用

```python
class ESCPOSCommands:
    def initialize(self) -> bytes: ...
    def align_center(self) -> bytes: ...
    def align_left(self) -> bytes: ...
    def align_right(self) -> bytes: ...
    def set_bold(self, enabled: bool) -> bytes: ...
    def set_font_size(self, size: int) -> bytes: ...
    def set_qr_size(self, size: int) -> bytes: ...
    def set_qr_error_correction(self, level: str) -> bytes: ...
    def print_qr(self, data: str) -> List[bytes]: ...
    def line_feed(self, lines: int = 1) -> bytes: ...
    def cut_paper(self, full_cut: bool = True) -> bytes: ...
```

### 10.1.3 蓝牙适配器抽象设计

```python
class BluetoothAdapter(ABC):
    @abstractmethod
    def discover(self) -> List[Device]:
        """发现附近的蓝牙设备"""
        pass

    @abstractmethod 
    def connect(self, address: str) -> bool:
        """连接到指定地址的蓝牙设备"""
        pass

    @abstractmethod
    def send(self, data: bytes) -> bool:
        """发送数据到已连接的设备"""
        pass

    @abstractmethod
    def disconnect(self) -> None:
        """断开当前连接"""
        pass

    @abstractmethod
    def is_connected(self) -> bool:
        """检查连接状态"""
        pass

    @abstractmethod
    def set_auto_reconnect(self, enabled: bool, max_retries: int = 3) -> None:
        """设置自动重连"""
        pass
```

### 10.2 各设备对接技术方案

#### 10.2.1 电子视力表对接

**通讯协议**: HTTP（RESTful 风格）

**对接方式**（双向支持）：

| 方式         | 说明            | 技术细节                                                   |
| ---------- | ------------- | ------------------------------------------------------ |
| **自动推送模式** | 设备测试完成后主动发送数据 | 系统启动 HTTP 服务监听端口（如 8181），<br>设备配置回调地址：`http://系统IP:端口` |
| **主动拉取模式** | 系统主动从设备获取数据   | 访问设备 IP:8181，支持 JSON、XLS、TXT 格式                        |

**电子视力表数据格式**（自动推送模式）：

```
http://{server-ip}:{port}?visionType={视标类型}&eyes={双眼视力}&right={右眼视力}&left={左眼视力}&resultTime={时间}&userName={姓名}&userId={ID}&deviceNumber={设备编号}
```

示例：

```
http://192.168.0.100:8181?visionType=E&eyes=2.0(5.3)&right=0.1(40)&left=1.2(5.1)&resultTime=2018-07-02-08-25-19&userName=张三&userId=12345468&deviceNumber=1
```

**电子视力表数据格式**（主动拉取模式，JSON）：

```json
{
  "Name": "患者姓名",
  "phone": "联系电话",
  "birthday": "生日",
  "gender": "性别",
  "SpaceType": "5m",
  "EyeVersion": "E",
  "Environment": "标高",
  "EyeCorrect": "裸眼",
  "TestMode": "正常测试",
  "OpenMirror": false,
  "DeviceName": "设备名称",
  "DateTime": "2025-08-15 16:43:34",
  "Result": {
    "OD": {
      "vision": "0.3",
      "logVision": "4.5",
      "subVision": "",
      "ref": "316.2",
      "isLowVision": false,
      "lowVision": "",
      "speed": "9s"
    },
    "OS": {
      "vision": "0.2",
      "logVision": "4.3",
      "subVision": "",
      "ref": "501.1",
      "isLowVision": false,
      "lowVision": "",
      "speed": "13s"
    }
  }
}
```

#### 10.2.2 眼生物测量仪对接

**通讯协议**: 蓝牙打印协议（SPP Profile）

**对接方式**：

- 系统模拟蓝牙打印机角色
- 设备通过蓝牙 SPP 发送打印数据流
- 系统解析打印数据流提取结构化数据

**注**：详细协议规范见 `生物测量仪蓝牙数据传输说明1.pdf`

#### 10.2.3 视力筛查仪对接

**通讯协议**: 蓝牙打印协议（SPP Profile）

**对接方式**：

- 系统模拟蓝牙打印机角色
- 设备通过蓝牙 SPP 发送打印数据流
- 系统解析打印数据流提取结构化数据

**注**：详细协议规范见 `视力筛查仪蓝牙数据接口-easyRef.pdf`

#### 10.2.4 其他设备（眼底照相机、电脑验光仪、眼压计）

**通讯协议**: 蓝牙打印协议（SPP Profile）

**对接方式**：

- 系统模拟蓝牙打印机角色
- 设备通过蓝牙 SPP 发送打印数据流
- 系统解析打印数据流提取结构化数据

**待补充**：需要获取这些设备的具体协议文档

### 10.3 统一数据格式设计

#### 10.3.1 患者基本信息

| 字段名         | 类型     | 说明    | 来源示例            |
| ----------- | ------ | ----- | --------------- |
| patientName | String | 患者姓名  | 电子视力表: userName |
| patientId   | String | 患者 ID | 电子视力表: userId   |
| phone       | String | 联系电话  | 电子视力表: phone    |
| birthday    | String | 出生日期  | 电子视力表: birthday |
| gender      | String | 性别    | 电子视力表: gender   |

#### 10.3.2 视力检查数据

| 字段名         | 类型       | 说明   | 来源示例                         |
| ----------- | -------- | ---- | ---------------------------- |
| checkTime   | DateTime | 检查时间 | 电子视力表: resultTime/DateTime   |
| deviceType  | String   | 设备类型 | "电子视力表" / "眼生物测量仪" 等         |
| deviceId    | String   | 设备编号 | 电子视力表: deviceNumber          |
| visionType  | String   | 视标类型 | 电子视力表: visionType/EyeVersion |
| spaceType   | String   | 检查距离 | 电子视力表: SpaceType             |
| environment | String   | 环境亮度 | 电子视力表: Environment           |
| eyeCorrect  | String   | 矫正方式 | 电子视力表: EyeCorrect（裸眼/矫正）     |

#### 10.3.3 眼部测量数据（右眼 OD / 左眼 OS）

| 字段名         | 类型      | 说明    | 来源示例（电子视力表）                                   |
| ----------- | ------- | ----- | --------------------------------------------- |
| vision      | String  | 视力值   | Result.OD.vision / Result.OS.vision           |
| logVision   | String  | 对数视力  | Result.OD.logVision / Result.OS.logVision     |
| ref         | String  | 参考值   | Result.OD.ref / Result.OS.ref                 |
| speed       | String  | 检查时长  | Result.OD.speed / Result.OS.speed             |
| isLowVision | Boolean | 是否低视力 | Result.OD.isLowVision / Result.OS.isLowVision |
| lowVision   | String  | 低视力说明 | Result.OD.lowVision / Result.OS.lowVision     |

**注**：其他设备（生物测量仪、验光仪等）的数据字段需根据协议文档补充

### 10.4 热敏打印机技术规格

| 规格项       | 参数              |
| --------- | --------------- |
| **打印指令集** | ESC/POS 标准指令集   |
| **打印纸宽度** | 56mm            |
| **连接方式**  | 蓝牙（SPP Profile） |
| **打印方式**  | 热敏打印            |

#### 10.4.1 ESC/POS 常用指令

| 指令                            | 说明            | 示例                     |
| ----------------------------- | ------------- | ---------------------- |
| `ESC @`                       | 初始化打印机        | `0x1B 0x40`            |
| `ESC ! n`                     | 设置打印模式（字体大小等） | `0x1B 0x21 0x00`（标准模式） |
| `ESC E n`                     | 设置/取消加粗       | `0x1B 0x45 0x01`（加粗开启） |
| `ESC a n`                     | 设置对齐方式        | `0x1B 0x61 0x01`（居中）   |
| `GS k m d1...dk NUL`          | 打印一维条码        |                        |
| `GS ( k pL pH cn fn m n data` | 打印二维码         |                        |
| `LF`                          | 换行            | `0x0A`                 |
| `GS V n`                      | 切纸            | `0x1D 0x56 0x00`（全切）   |

#### 10.4.2 打印模板设计（56mm 宽度）

```
┌────────────────────────────────┐
│     XX 视力中心检查报告        │  ← 居中，标题，字体放大
├────────────────────────────────┤
│ 姓名：张三                      │
│ 时间：2026-05-23 15:30         │
├────────────────────────────────┤
│                                │
│  ▶ 电子视力表                  │  ← 设备类型
│                                │
│      右眼 (OD)   左眼 (OS)     │
│ 视力   0.3        0.2          │
│ 对数  4.5        4.3          │
│                                │
│  ──────────────────────────    │
│                                │
│  ▶ 眼生物测量仪                │  ← 其他设备数据
│  ...（按设备依次展示）...      │
│                                │
├────────────────────────────────┤
│                                │
│     ██████████████            │  ← 二维码（居中）
│     ██████████████            │
│     ██████████████            │
│                                │
│  扫码查看完整检查数据          │  ← 底部提示
│                                │
└────────────────────────────────┘
```

**布局规格**（56mm 宽度，约 48 字符）：

- 标题：居中，字体放大 2 倍
- 正文：标准字体，每行 24-32 汉字
- 二维码：居中，模块大小 6-8dot，版本 2-3（约 1.5-2cm 见方）
- 边距：左右各约 3mm

#### 10.4.3 二维码打印（ESC/POS）

使用 QR Code 打印指令：

```
GS ( k pL pH cn fn m n data
```

参数说明：

- `cn`: 符号类型（`49` = QR Code）
- `fn`: 功能编号（`80` = 打印存储的 QR Code）
- `m`: 模块大小（`6` = 6dot/模块）
- `n`: 纠错等级（`48` = L, `49` = M, `50` = Q, `51` = H）

### 10.5 二维码数据结构

二维码采用 **JSON 格式编码**，包含以下信息：

```json
{
  "version": "1.0",
  "generateTime": "2026-05-23T15:30:00",
  "patientInfo": {
    "name": "张三",
    "id": "12345678",
    "phone": "13800138000"
  },
  "checkData": [
    {
      "deviceType": "电子视力表",
      "checkTime": "2026-05-23T15:25:00",
      "data": {
        // 各设备的具体检查数据
      }
    }
    // 可包含多个设备的检查数据
  ]
}
```

### 10.6 数据流程说明

#### 10.6.1 电子视力表数据流程（自动推送模式）

```
┌──────────┐   1. 测试完成   ┌─────────────────┐   2. HTTP GET 请求    ┌─────────────────┐
│ 电子视力表 │ ──────────────> │ 设备配置回调地址 │ ──────────────────> │ 系统 HTTP 服务  │
└──────────┘                 └─────────────────┘                      └────────┬────────┘
                                                                              │ 3. 解析 URL 参数
                                                                              │ 4. 映射到统一数据模型
                                                                              ▼
┌─────────────────┐   7. 打印完成反馈    ┌─────────────────┐   5. 存入暂存区    ┌─────────────────┐
│ 蓝牙热敏打印机  │ <────────────────── │ 打印服务         │ <──────────────── │ 数据处理服务    │
└─────────────────┘                      └─────────────────┘   6. 触发打印（可选）└─────────────────┘
```

**详细步骤**：
1. 医护人员使用电子视力表为患者完成视力测试
2. 电子视力表自动向预设的回调地址发送 HTTP GET 请求，携带测试数据
3. 系统 HTTP 服务接收请求，解析 URL 参数
4. 数据处理服务将数据映射到统一数据模型
5. 数据存入本地暂存区，同时触发数据接收事件
6. （可选）如果配置了自动打印，打印服务自动生成打印任务
7. 蓝牙热敏打印机执行打印，输出标准化检查报告

#### 10.6.2 蓝牙打印协议设备数据流程（以眼生物测量仪为例）

```
┌──────────────┐   1. 测试完成   ┌─────────────────┐   2. 蓝牙 SPP 连接   ┌─────────────────┐
│ 眼生物测量仪  │ ──────────────> │ 发送打印数据流   │ ──────────────────> │ 蓝牙适配器      │
└──────────────┘                 └─────────────────┘                      └────────┬────────┘
                                                                              │ 3. 接收打印流数据
                                                                              ▼
┌─────────────────┐   7. 打印完成反馈    ┌─────────────────┐   6. 触发打印（可选）┌─────────────────┐
│ 蓝牙热敏打印机  │ <────────────────── │ 打印服务         │ <──────────────── │ 数据解析服务    │
└─────────────────┘                      └─────────────────┘                      └────────┬────────┘
                                                                              │ 5. 映射到统一数据模型
                                                                              │ 4. 解析打印流提取结构化数据
                                                                              ▼
                                                                         ┌─────────────────┐
                                                                         │ 本地数据暂存区   │
                                                                         └─────────────────┘
```

**详细步骤**：
1. 医护人员使用眼生物测量仪为患者完成检查
2. 设备通过蓝牙 SPP 向系统发送打印数据流（系统模拟打印机角色）
3. 蓝牙适配器接收原始打印流数据
4. 数据解析服务从打印流中提取结构化数据（患者信息、测量结果等）
5. 映射到统一数据模型
6. 数据存入本地暂存区，（可选）自动触发打印
7. 蓝牙热敏打印机输出标准化报告

#### 10.6.3 多设备数据合并打印流程

```
┌──────────────┐
│ 电子视力表    │ ──数据──┐
└──────────────┘         │
┌──────────────┐         ▼
│ 电脑验光仪    │ ──数据──> ┌─────────────────┐    等待时间窗口    ┌─────────────────┐
└──────────────┘         │ │ 数据暂存区       │ ───────────────> │ 数据合并服务    │
┌──────────────┐         │ └─────────────────┘                   └────────┬────────┘
│ 眼生物测量仪  │ ──数据──┘                                              │
└──────────────┘                                                         │ 合并多设备数据
                                                                         ▼
┌─────────────────┐   打印完成反馈    ┌─────────────────┐   生成打印任务    ┌─────────────────┐
│ 蓝牙热敏打印机  │ <────────────── │ 打印服务         │ <────────────── │ 统一报告生成    │
└─────────────────┘                   └─────────────────┘                   └─────────────────┘
```

**详细步骤**：
1. 患者依次完成多个设备检查，各设备数据陆续到达系统
2. 数据暂存区按患者维度临时存储
3. 系统等待预设时间窗口（如 5 分钟）或手动触发合并
4. 数据合并服务将同一患者的多个设备数据整合
5. 统一报告生成器生成包含所有检查数据的完整报告
6. 打印服务执行打印
7. 输出包含多设备数据的综合检查报告

### 10.7 系统使用流程

#### 10.7.1 首次部署配置流程

```
1. 系统启动
   ↓
2. 进入配置向导
   ↓
3. 添加设备配置
   ├─ 电子视力表：配置 HTTP 监听端口、设备 IP
   ├─ 蓝牙设备：选择设备类型、搜索配对
   └─ 热敏打印机：搜索配对蓝牙打印机
   ↓
4. 配置打印参数
   ├─ 选择打印模板
   ├─ 设置自动打印开关
   └─ 配置二维码参数
   ↓
5. 测试连接
   ├─ 测试各设备连接状态
   └─ 打印测试页
   ↓
6. 保存配置，完成部署
```

#### 10.7.2 日常使用流程（医生视角）

```
场景：患者到视力中心做完整检查

1. 患者登记（可选，如电子视力表支持直接录入）
   ↓
2. 第一项检查（如电子视力表）
   ├─ 医生操作设备完成检查
   ├─ 设备自动发送数据到系统
   └─ 系统接收并暂存数据
   ↓
3. 第二项检查（如电脑验光仪）
   ├─ 医生操作设备完成检查
   ├─ 设备自动发送数据到系统
   └─ 系统接收并暂存数据
   ↓
4. 继续其他检查...（生物测量仪、眼压计等）
   ↓
5. 所有检查完成
   ├─ 医生在系统界面查看汇总数据
   ├─ 确认数据无误
   └─ 点击"打印报告"
   ↓
6. 系统打印综合报告
   ├─ 包含所有检查数据
   ├─ 统一格式
   └─ 带数据二维码
   ↓
7. 报告交给患者，检查完成
```

#### 10.7.3 异常处理流程

**场景 1：设备连接断开**
```
1. 系统检测到设备断开
   ↓
2. 自动触发重连机制（按配置策略）
   ↓
3. 界面显示"设备离线，正在重连..."
   ↓
4. 重连成功：恢复正常状态
   重连失败：显示失败提示，记录日志，提示手动检查
```

**场景 2：数据解析失败**
```
1. 接收到设备数据
   ↓
2. 解析失败
   ↓
3. 记录原始数据到日志（用于排查）
   ↓
4. 界面提示"数据解析异常，请检查设备或联系技术支持"
   ↓
5. 保留原始数据，支持后续人工处理
```

**场景 3：打印失败**
```
1. 发送打印任务
   ↓
2. 打印失败（缺纸/连接断开/打印机故障）
   ↓
3. 系统记录失败状态
   ↓
4. 自动重试（按配置次数）
   ↓
5. 重试成功：恢复正常
   重试失败：提示具体原因（如"打印机缺纸"），任务保留在队列中
   ↓
6. 问题解决后，支持手动重新打印
```

### 10.8 接口设计（API 层）

#### 10.8.1 电子视力表接收接口（HTTP GET）

```
GET /api/receive/vision-chart
```

**请求参数**（URL Query）：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| visionType | String | 否 | 视标类型 |
| eyes | String | 否 | 双眼视力 |
| right | String | 否 | 右眼视力 |
| left | String | 否 | 左眼视力 |
| resultTime | String | 是 | 检查时间（格式：YYYY-MM-DD-HH-mm-ss） |
| userName | String | 是 | 患者姓名 |
| userId | String | 是 | 患者 ID |
| deviceNumber | String | 是 | 设备编号 |

**响应示例**：
```json
{
  "code": 0,
  "message": "接收成功",
  "data": {
    "recordId": "rec_001"
  }
}
```

#### 10.8.2 打印接口（RESTful API）

```
POST /api/print
```

**请求体**：
```json
{
  "recordIds": ["rec_001", "rec_002"],
  "template": "full",
  "autoPrint": true
}
```

**响应示例**：
```json
{
  "code": 0,
  "message": "打印任务已创建",
  "data": {
    "taskId": "task_001",
    "status": "pending"
  }
}
```

#### 10.8.3 数据查询接口

```
GET /api/records
```

**查询参数**：
| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| startTime | String | 否 | 开始时间 |
| endTime | String | 否 | 结束时间 |
| patientName | String | 否 | 患者姓名（模糊查询） |
| patientId | String | 否 | 患者 ID |
| deviceType | String | 否 | 设备类型 |
| page | Int | 否 | 页码，默认 1 |
| pageSize | Int | 否 | 每页数量，默认 20 |

**响应示例**：
```json
{
  "code": 0,
  "data": {
    "total": 100,
    "records": [
      {
        "id": "rec_001",
        "patientName": "张三",
        "deviceType": "电子视力表",
        "checkTime": "2026-05-23T15:30:00",
        "status": "completed"
      }
    ]
  }
}
```

#### 10.8.4 设备管理接口

```
GET    /api/devices              # 获取设备列表
POST   /api/devices              # 添加设备
PUT    /api/devices/{id}         # 更新设备配置
DELETE /api/devices/{id}         # 删除设备
POST   /api/devices/{id}/test    # 测试设备连接
```

#### 10.8.5 系统状态接口

```
GET /api/status
```

**响应示例**：
```json
{
  "code": 0,
  "data": {
    "systemStatus": "running",
    "devices": [
      {
        "id": "dev_001",
        "name": "电子视力表",
        "type": "vision-chart",
        "status": "connected",
        "lastDataTime": "2026-05-23T15:30:00"
      },
      {
        "id": "dev_002",
        "name": "热敏打印机",
        "type": "printer",
        "status": "connected",
        "paperStatus": "normal"
      }
    ],
    "queueStatus": {
      "pending": 0,
      "printing": 0,
      "failed": 0
    }
  }
}
```

---

## 11. 更新日志

| 版本   | 日期         | 说明                                                                                      |
| ---- | ---------- | --------------------------------------------------------------------------------------- |
| v3.0 | 2026-05-23 | **重大更新**：完善系统功能需求说明，新增详细功能描述、性能需求、安全合规需求、用户体验需求、运维监控需求；新增数据流程说明、系统使用流程、接口设计 |
| v2.0 | 2026-05-23 | **重大更新**：新增历史项目经验总结（PrintServerV5 问题回顾）、补充分层架构设计、新增代码质量强制要求（REQ-020 至 REQ-028）、完善风险应对策略 |
| v1.2 | 2026-05-23 | 新增热敏打印机技术规格章节，明确 ESC/POS 指令集、56mm 打印纸宽度、打印模板设计                                          |
| v1.1 | 2026-05-23 | 新增技术方案章节，补充电子视力表 HTTP 协议对接细节、统一数据格式设计、二维码结构                                             |
| v1.0 | 2026-05-23 | 初始版本发布                                                                                  |

---

**文档结束**

---

*本 PRD 为产品需求的基础版本，后续将根据项目进展和用户反馈持续迭代更新。*
